---
paths:
  - src/**/*.{ts,tsx}
---

# コーディング規約

- コンポーネントは関数コンポーネント（`const Foo: React.FC = () => {}`）
- ファイル名はコンポーネントはPascalCase（`ShiftTable.tsx`）、それ以外はcamelCase
- 型定義は`src/types/`に集約
- コメントは日本語で記述

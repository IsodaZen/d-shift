## 1. テスト作成（Red）

- [ ] 1.1 `ShiftTable.test.tsx` に時間帯集計行の表示テストを追加する（Red）
  - 集計行が時間帯ごとに3行（午前・午後・夕方）表示されるテスト
  - アサイン数と必要人数が「3/6」形式で表示されるテスト
  - アサイン数が必要人数を下回る場合にオレンジ系クラスが付与されるテスト
  - アサイン数が0の場合も警告色が付与されるテスト
  - アサイン数が必要人数以上の場合に警告色が付与されないテスト
  - 必要人数が0（祝日デフォルト）の場合に通常色で表示されるテスト
  - ヘルプスタッフのアサインが集計行に含まれないテスト
  - `getRequiredCount` 未渡し時に「N/-」形式で表示されるテスト

## 2. 実装（Green）

- [ ] 2.1 `ShiftTable.tsx` に時間帯別アサイン数を計算するロジックを追加する（Green）
  - 通常スタッフのみのアサイン数を日付・時間帯ごとに算出するヘルパー関数を追加する
  - `staffIds`（通常スタッフのIDセット）を参照してヘルプスタッフを除外する

- [ ] 2.2 `ShiftTable.tsx` の `<tbody>` 先頭に時間帯別集計行を追加する（Green）
  - 午前・午後・夕方の3行を追加する
  - 各行の左端セル（sticky固定列）に時間帯ラベルを表示する
  - 各日付セルに「アサイン数/必要人数」（または `getRequiredCount` 未渡し時は「N/-」）を表示する
  - アサイン数 < 必要人数 の場合はオレンジ系クラス（`bg-orange-50 text-orange-700`）を適用する
  - アサイン数 >= 必要人数（必要人数0を含む）の場合は通常グレー系クラスを適用する

## 3. リファクタリング（Refactor）

- [ ] 3.1 集計ロジックを `useCallback` でメモ化してパフォーマンスを最適化する（Refactor）
- [ ] 3.2 テストが全件グリーンであることを確認し、コードを整理する（Refactor）

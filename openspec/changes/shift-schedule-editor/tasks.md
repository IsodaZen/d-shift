## 1. 環境・プロジェクト基盤

- [x] 1.1 date-fnsをインストールする（`bun add date-fns`）
- [x] 1.2 srcディレクトリ構成を整備する（components/, hooks/, pages/, types/, utils/ の各フォルダ確認）
- [x] 1.3 ボトムナビゲーション付きのAppShellコンポーネントを作成する（タブ: シフト表 / スタッフ / 設定）
- [x] 1.4 React RouterまたはシンプルなState管理でタブ切り替えを実装する

## 2. 型定義

- [x] 2.1 `Staff` 型を定義する（id, name, maxWeeklyShifts, availableSlots, usesParking）
- [x] 2.2 `TimeSlot` 型を定義する（"morning" | "afternoon" | "evening"）
- [x] 2.3 `ShiftSlotConfig` 型を定義する（date, timeSlot, requiredCount）
- [x] 2.4 `PreferredDayOff` 型を定義する（id, staffId, date）
- [x] 2.5 `ShiftAssignment` 型を定義する（id, staffId, date, timeSlot, parkingSpot）
- [x] 2.6 `ParkingConfig` 型を定義する（slots: { type: "A" | "B", count: number }[]）

## 3. LocalStorageフック

- [x] 3.1 `useLocalStorage<T>` ジェネリックフックを作成する（読み書き・JSON変換）
- [x] 3.2 `useStaff` フックを作成する（CRUD操作 + LocalStorage同期）
- [x] 3.3 `useShiftConfig` フックを作成する（必要人数の取得・更新 + LocalStorage同期）
- [x] 3.4 `useDayOffs` フックを作成する（希望休のCRUD + LocalStorage同期）
- [x] 3.5 `useAssignments` フックを作成する（シフト割り当てのCRUD + LocalStorage同期）
- [x] 3.6 `useParkingConfig` フックを作成する（駐車場枠設定の取得・更新 + LocalStorage同期、初期値A×4・B×1）

## 4. スタッフ管理

- [x] 4.1 `StaffForm` コンポーネントを作成する（氏名・週上限回数・出勤可能時間帯チェックボックス・駐車場利用トグル）
- [x] 4.2 `StaffList` コンポーネントを作成する（スタッフ一覧表示・編集ボタン・削除ボタン）
- [x] 4.3 スタッフ登録フォームのバリデーションを実装する（氏名必須・週上限1以上）
- [x] 4.4 スタッフ管理ページ（`StaffPage`）を作成し、一覧と登録/編集フォームを統合する
- [x] 4.5 スタッフ0件時の空状態メッセージを表示する

## 5. シフト枠・設定

- [x] 5.1 各日・時間帯（午前/午後/夕方）の必要人数を入力できる `ShiftConfigForm` コンポーネントを作成する
- [x] 5.2 必要人数のバリデーションを実装する（1以上）
- [x] 5.3 駐車場枠設定UI（種別A/Bの枠数変更）を作成する
- [x] 5.4 設定ページ（`SettingsPage`）を作成し、シフト枠設定と駐車場設定を統合する

## 6. 希望休管理

- [x] 6.1 希望休登録UI（スタッフ選択 + 日付選択）を作成する
- [x] 6.2 希望休の重複登録チェックを実装する
- [x] 6.3 希望休一覧コンポーネント（`DayOffList`）を作成する（スタッフ名・日付・削除ボタン）
- [x] 6.4 希望休管理を設定ページまたは専用セクションに組み込む

## 7. シフト割り当てロジック

- [x] 7.1 駐車場自動割り当てユーティリティを作成する（その日の空き枠をA優先で先着順に割り当て）
- [x] 7.2 週上限出勤回数チェックユーティリティを作成する（staffId + 週の開始日 → 現在のアサイン数）
- [x] 7.3 出勤可能時間帯チェックユーティリティを作成する（staffId + timeSlot → 可否）
- [x] 7.4 シフトアサイン処理（アサイン追加・削除・保存）を `useAssignments` に統合する

## 8. シフト表表示

- [x] 8.1 週ナビゲーションコンポーネントを作成する（前の週・次の週ボタン + 表示期間ラベル）
- [x] 8.2 シフト表テーブルコンポーネント（`ShiftTable`）を作成する（行: スタッフ、列: 日付、スタッフ列を固定）
- [x] 8.3 セルコンポーネントを作成する（時間帯バッジ・希望休ハイライト・希望休アサイン赤ハイライト・駐車場番号表示）
- [x] 8.4 時間帯バッジの表示（午前/午後/夕方のバッジ）を実装する
- [x] 8.5 希望休セルのハイライト表示（黄色）を実装する
- [x] 8.6 希望休日アサインのハイライト表示（赤）を実装する
- [x] 8.7 駐車場番号をセルに表示する（例: A1, B1）
- [x] 8.8 横スクロール対応のレスポンシブレイアウトをTailwind CSSで実装する

## 9. シフトアサインUI

- [x] 9.1 セルタップで時間帯選択モーダルまたはドロワーを表示する（午前/午後/夕方のトグル）
- [x] 9.2 アサイン時に週上限チェックを実行し、超過時に警告トーストを表示する
- [x] 9.3 アサイン時に出勤不可時間帯チェックを実行し、不可時に警告トーストを表示する

## 10. ヘルプスタッフアラート

- [x] 10.1 `useHelpAlert` フックを作成する（日付・時間帯ごとの不足人数を算出）
- [x] 10.2 シフト表の各日・時間帯の列ヘッダーまたはセルに「ヘルプ要」バッジと不足人数を表示する

## 11. 品質・仕上げ

- [x] 11.1 スマートフォン（モバイルファースト）でのレイアウト確認と調整をする
- [x] 11.2 `bun run build` でビルドエラーがないことを確認する
- [x] 11.3 TypeScriptの型エラーがないことを確認する（`tsc --noEmit`）

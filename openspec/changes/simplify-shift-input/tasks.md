## 1. ShiftTable の通常スタッフセルクリック動作をリファクタリング

- [ ] 1.1 `ShiftTable.tsx` の通常スタッフセル一括アサインに関するテストを書く（Red）
  - アサインなしセルをクリック → `availableSlots` の全時間帯が `onAddAssignment` で呼ばれることを確認
  - `availableSlots` が空のセルをクリック → 全時間帯（`ALL_TIME_SLOTS`）が `onAddAssignment` で呼ばれることを確認
  - アサインありセルをクリック → `onRemoveAssignment` が全時間帯分呼ばれることを確認
- [ ] 1.2 `ShiftTable.tsx` の通常スタッフセルクリック時の一括トグル処理を実装する（Green）
  - モーダル表示を廃止し、セルクリックで直接 `handleBulkToggle` を呼び出す
  - アサインなし時: `staff.availableSlots`（空なら `ALL_TIME_SLOTS`）を全時間帯アサイン
  - アサインあり時: 当日の全アサインを一括削除（`isLocked` 問わず）
- [ ] 1.3 週上限チェックロジックのテストを書く（Red）
  - アサイン追加後に週上限を超える場合、`onAddAssignment` が呼ばれず警告トーストが表示されることを確認
  - 週上限未満の場合、アサインが正常に保存されることを確認
- [ ] 1.4 週上限チェックを「警告のみ」から「ブロック」に変更する（Green）
  - 現在の「週上限を超えます → 警告のみ」の実装を「ブロック → 保存しない」に修正
- [ ] 1.5 出勤不可時間帯の警告トーストのテストを書く（Red）
  - `availableSlots` に含まれない時間帯があるスタッフをクリックしたとき警告トーストが表示されることを確認
  - アサイン自体は保存されることを確認
- [ ] 1.6 出勤不可時間帯の警告トースト表示を実装する（Green）
  - 一括アサイン実行後、`availableSlots` に含まれない時間帯があれば警告トーストを表示

## 2. モーダル関連のクリーンアップ

- [ ] 2.1 `ShiftTable.tsx` から通常スタッフ向け `AssignModal` の表示コードを削除する
  - `modalStaff` を参照していたモーダル部分を除去
  - `handleToggleSlot` 関数を削除（ヘルプスタッフ用 `handleToggleHelpSlot` は維持）
  - 通常スタッフ用モーダル `ModalState` の `isHelpStaff` フラグを整理（ヘルプスタッフ専用に変更）
- [ ] 2.2 `AssignModal` コンポーネントのテストが引き続きパスすることを確認（ヘルプスタッフ用途は維持）

## 3. テストの更新

- [ ] 3.1 `ShiftTable.test.tsx` の通常スタッフセルクリック関連のテストをすべて更新する
  - モーダルが表示されることを期待するテストを削除・修正
  - 一括アサイン挙動を期待するテストに書き換える
- [ ] 3.2 既存の週上限警告テスト（`ShiftTable.test.tsx` 内）を「ブロック」挙動に合わせて更新する

## 4. 動作確認とリファクタリング

- [ ] 4.1 `bun run test` を実行して全テストがパスすることを確認（Refactor）
- [ ] 4.2 `bun run build` を実行してビルドエラーがないことを確認
